version: "3.8"

services:
  wyoming-piper:
    build: .
    environment:
      - VOICE=en_US-hfc_female-medium
      - UPDATE_VOICES=1
      # Optional:
      # - USE_CUDA=1
      # - NO_STREAMING=1
      # - SPEAKER=
      # - NOISE_SCALE=
      # - LENGTH_SCALE=
      # - NOISE_W_SCALE=
      # - SAMPLES_PER_CHUNK=
    ports:
      - "10200:10200"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
